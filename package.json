{
  "devDependencies": {
    "babel-preset-es2015": "^6.24.1",
    "babel-register": "^6.26.0",
    "esbuild": "^0.12.20",
    "node-wav": "0.0.2",
    "servor": "3.1.0",
    "tape": "^4.13.0"
  },
  "scripts": {
    "start": "servor . index.html 8080 --reload --browse",
    "build": "./node_modules/esbuild/bin/esbuild app.js --minify --bundle --outfile=build/index.js",
    "test": "tape -r babel-register tests/*.js"
  },
  "dependencies": {
    "kalmanjs": "^1.1.0"
  }
}
